# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=False
ENVIRONMENT=development
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173,http://localhost:3001,http://127.0.0.1:3001

# Database Configuration
DATABASE_URL=sqlite:///virtual_tryon.db


# 3D Model Processing
MODEL_CACHE_DIR=./cache
MAX_PROCESSING_TIME=60

# Upload settings
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=10485760  # 10MB

# Result settings
RESULT_DIR=./results
RESULT_EXPIRY_DAYS=7

# Proxy settings (for API calls)
USE_PROXY=False
PROXY_URL=

# Cache settings
CACHE_ENABLED=True
CACHE_DIR=./cache
CACHE_EXPIRY_HOURS=24

# Logging
LOG_LEVEL=INFO
LOG_DIR=./logs
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Virtual Try-On Configuration
TRYON_MODEL_PATH=./models/tryon_model
SEGMENTATION_MODEL_PATH=./models/segmentation_model 
